# Copy this to .env.local and fill in values for your environment.
# .env.local is gitignored — never commit real URLs or secrets.

# Leave blank for local dev (invoices save to localStorage only).
# Set to the API Gateway URL to connect to a real DynamoDB instance.
VITE_INVOICES_API_URL=
